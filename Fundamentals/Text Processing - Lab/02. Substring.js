function substring(string, start, count) {
    
    console.log(string.substring(start, start + count));
    // взимаме подстринг на стринга, за начало даваме от къде да почва, за край даваме къде свършва
}
substring("ASentence", 1, 8);
substring("SkipWord", 4, 7);
