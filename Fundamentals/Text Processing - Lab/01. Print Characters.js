function printCharacters(string) {

    for (let char of string) {      // Минаваме по всяка буква от стринга
        console.log(char);          // я отпечатваме
    }
}
printCharacters("Sentence");
//printCharacters('AWord')
