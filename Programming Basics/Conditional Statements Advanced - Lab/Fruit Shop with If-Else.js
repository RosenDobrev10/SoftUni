function fruitShop(input) {
    let food = input[0];
    let day = input[1];
    let amount = Number(input[2]);
    let price = 0.0;

    if (day === "Monday" && food === "banana") {
        price = amount * 2.5;
        console.log(price.toFixed(2));
    } else if (day === "Monday" && food === "apple") {
        price = amount * 1.2;
        console.log(price.toFixed(2));
    } else if (day === "Monday" && food === "orange") {
        price = amount * 0.85;
        console.log(price.toFixed(2));
    } else if (day === "Monday" && food === "grapefruit") {
        price = amount * 1.45;
        console.log(price.toFixed(2));
    } else if (day === "Monday" && food === "kiwi") {
        price = amount * 2.7;
        console.log(price.toFixed(2));
    } else if (day === "Monday" && food === "pineapple") {
        price = amount * 5.5;
        console.log(price.toFixed(2));
    } else if (day === "Monday" && food === "grapes") {
        price = amount * 3.85;
        console.log(price.toFixed(2));
    } else if (day === "Tuesday" && food === "banana") {
        price = amount * 2.5;
        console.log(price.toFixed(2));
    } else if (day === "Tuesday" && food === "apple") {
        price = amount * 1.2;
        console.log(price.toFixed(2));
    } else if (day === "Tuesday" && food === "orange") {
        price = amount * 0.85;
        console.log(price.toFixed(2));
    } else if (day === "Tuesday" && food === "grapefruit") {
        price = amount * 1.45;
        console.log(price.toFixed(2));
    } else if (day === "Tuesday" && food === "kiwi") {
        price = amount * 2.7;
        console.log(price.toFixed(2));
    } else if (day === "Tuesday" && food === "pineapple") {
        price = amount * 5.5;
        console.log(price.toFixed(2));
    } else if (day === "Tuesday" && food === "grapes") {
        price = amount * 3.85;
        console.log(price.toFixed(2));
    } else if (day === "Wednesday" && food === "banana") {
        price = amount * 2.5;
        console.log(price.toFixed(2));
    } else if (day === "Wednesday" && food === "apple") {
        price = amount * 1.2;
        console.log(price.toFixed(2));
    } else if (day === "Wednesday" && food === "orange") {
        price = amount * 0.85;
        console.log(price.toFixed(2));
    } else if (day === "Wednesday" && food === "grapefruit") {
        price = amount * 1.45;
        console.log(price.toFixed(2));
    } else if (day === "Wednesday" && food === "kiwi") {
        price = amount * 2.7;
        console.log(price.toFixed(2));
    } else if (day === "Wednesday" && food === "pineapple") {
        price = amount * 5.5;
        console.log(price.toFixed(2));
    } else if (day === "Wednesday" && food === "grapes") {
        price = amount * 3.85;
        console.log(price.toFixed(2));
    } else if (day === "Thursday" && food === "banana") {
        price = amount * 2.5;
        console.log(price.toFixed(2));
    } else if (day === "Thursday" && food === "apple") {
        price = amount * 1.2;
        console.log(price.toFixed(2));
    } else if (day === "Thursday" && food === "orange") {
        price = amount * 0.85;
        console.log(price.toFixed(2));
    } else if (day === "Thursday" && food === "grapefruit") {
        price = amount * 1.45;
        console.log(price.toFixed(2));
    } else if (day === "Thursday" && food === "kiwi") {
        price = amount * 2.7;
        console.log(price.toFixed(2));
    } else if (day === "Thursday" && food === "pineapple") {
        price = amount * 5.5;
        console.log(price.toFixed(2));
    } else if (day === "Thursday" && food === "grapes") {
        price = amount * 3.85;
        console.log(price.toFixed(2));
    } else if (day === "Friday" && food === "banana") {
        price = amount * 2.5;
        console.log(price.toFixed(2));
    } else if (day === "Friday" && food === "apple") {
        price = amount * 1.2;
        console.log(price.toFixed(2));
    } else if (day === "Friday" && food === "orange") {
        price = amount * 0.85;
        console.log(price.toFixed(2));
    } else if (day === "Friday" && food === "grapefruit") {
        price = amount * 1.45;
        console.log(price.toFixed(2));
    } else if (day === "Friday" && food === "kiwi") {
        price = amount * 2.7;
        console.log(price.toFixed(2));
    } else if (day === "Friday" && food === "pineapple") {
        price = amount * 5.5;
        console.log(price.toFixed(2));
    } else if (day === "Friday" && food === "grapes") {
        price = amount * 3.85;
        console.log(price.toFixed(2));
    } else if (day === "Saturday" && food === "banana") {
        price = amount * 2.7;
        console.log(price.toFixed(2));
    } else if (day === "Saturday" && food === "apple") {
        price = amount * 1.25;
        console.log(price.toFixed(2));
    } else if (day === "Saturday" && food === "orange") {
        price = amount * 0.9;
        console.log(price.toFixed(2));
    } else if (day === "Saturday" && food === "grapefruit") {
        price = amount * 1.6;
        console.log(price.toFixed(2));
    } else if (day === "Saturday" && food === "kiwi") {
        price = amount * 3.0;
        console.log(price.toFixed(2));
    } else if (day === "Saturday" && food === "pineapple") {
        price = amount * 5.6;
        console.log(price.toFixed(2));
    } else if (day === "Saturday" && food === "grapes") {
        price = amount * 4.2;
        console.log(price.toFixed(2));
    } else if (day === "Sunday" && food === "banana") {
        price = amount * 2.7;
        console.log(price.toFixed(2));
    } else if (day === "Sunday" && food === "apple") {
        price = amount * 1.25;
        console.log(price.toFixed(2));
    } else if (day === "Sunday" && food === "orange") {
        price = amount * 0.9;
        console.log(price.toFixed(2));
    } else if (day === "Sunday" && food === "grapefruit") {
        price = amount * 1.6;
        console.log(price.toFixed(2));
    } else if (day === "Sunday" && food === "kiwi") {
        price = amount * 3.0;
        console.log(price.toFixed(2));
    } else if (day === "Sunday" && food === "pineapple") {
        price = amount * 5.6;
        console.log(price.toFixed(2));
    } else if (day === "Sunday" && food === "grapes") {
        price = amount * 4.2;
        console.log(price.toFixed(2));
    } else {
        console.log("error");
    }
}
fruitShop(["apple", "Tuesday", "2"]);
